import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.capnativebuild',
  appName: 'cap-native-build',
  webDir: 'www'
};

export default config;
